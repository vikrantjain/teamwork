---
max_turns: 12
allowed_tools: [Read, Glob, Grep, Skill]
---

Set up a team for this. We keep our plan current, so it should have everything
you need.

`ROADMAP.md` names four workstreams and says all four can start now: the ingest
service under `ingest/`, the query API under `api/`, the React console under
`console/`, and the operator runbooks under `docs/ops/`. Each one lists the files
it will write, and none of them waits on another.

Propose the team. Do not write any files.
